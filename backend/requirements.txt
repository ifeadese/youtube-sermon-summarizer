# Direct runtime dependencies (human-readable manifest), pinned to exact
# versions. For a fully reproducible install incl. transitive deps, use
# requirements.lock (the deploy build installs from that). See README → Deploy.
fastapi==0.136.3
uvicorn[standard]==0.49.0
youtube-transcript-api==1.2.4
anthropic==0.109.1
python-dotenv==1.2.2
